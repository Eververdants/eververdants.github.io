name = "eververdants-content-api"
main = "workers/content-api.js"
compatibility_date = "2024-01-01"

# KV 命名空间绑定
[[kv_namespaces]]
binding = "CONTENT_KV"
id = "e30ea316c3924e3089cb6dd7015bacdd"  # 需要替换为实际的 KV ID

# 路由配置（可选，如果你想用自定义域名）
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
