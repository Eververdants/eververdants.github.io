export default function ResumeScene() {
  return (
    <section className="flex h-dvh items-center justify-center px-[clamp(14px,3vw,32px)] text-center">
      <h2
        className="rv-in select-none whitespace-nowrap font-fraunces font-medium leading-none tracking-[0.03em] text-white text-[clamp(72px,16vw,240px)]"
        aria-label="Resume"
      >
        RESUME
      </h2>
    </section>
  );
}
